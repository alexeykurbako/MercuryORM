package sqlgenerator;

import org.junit.jupiter.api.Test;
import query.SQLAssistent;

public class SQLGeneratorTest {

        @Test
        public void shouldTransformatorReturnTrue() throws ClassNotFoundException {
            SQLAssistent assistent = new SQLAssistent(); // MyClass is tested


//            Map<String, String> ss = assistent.transformMethodNameToQueryParts("findByIdAndLogin", null);
//            System.out.println(ss);
        }
}
